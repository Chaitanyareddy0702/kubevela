{{- if .Values.multicluster.enabled }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clusterregistrations.core.oam.dev
spec:
  group: core.oam.dev
  names:
    categories:
    - oam
    kind: ClusterRegistration
    listKind: ClusterRegistrationList
    plural: clusterregistrations
    shortNames:
    - clusterreg
    singular: clusterregistration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterName
      name: CLUSTER
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.clusterInfo.endpoint
      name: ENDPOINT
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterRegistration is the Schema for the clusterregistrations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterRegistrationSpec defines the desired state of ClusterRegistration
            properties:
              alias:
                description: Alias is a human-readable name for the cluster
                type: string
              clusterName:
                description: |-
                  ClusterName is the name of the cluster to register
                  If empty, defaults to metadata.name
                type: string
              createNamespace:
                description: |-
                  CreateNamespace specifies the namespace to create in the managed cluster
                  Defaults to "vela-system"
                type: string
              kubeconfig:
                description: |-
                  Kubeconfig is the raw kubeconfig content for the cluster to be joined
                  This is a simplified POC approach where we just paste the kubeconfig directly
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels are custom labels to add to the cluster
                type: object
            required:
            - kubeconfig
            type: object
          status:
            description: ClusterRegistrationStatus defines the observed state of ClusterRegistration
            properties:
              clusterInfo:
                description: ClusterInfo contains information about the registered
                  cluster
                properties:
                  credentialType:
                    description: CredentialType is the type of credential used
                    type: string
                  endpoint:
                    description: Endpoint is the API server endpoint
                    type: string
                  version:
                    description: Version is the Kubernetes version of the cluster
                    type: string
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the cluster registration state
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastReconcileTime:
                description: LastReconcileTime is the last time the cluster registration
                  was reconciled
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this ClusterRegistration
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of cluster registration
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
